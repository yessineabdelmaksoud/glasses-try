# Environment Configuration for Virtual Glasses Try-On
# Copy this file to .env and update the values according to your setup

# MySQL Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=virtual_glasses
DB_PORT=3306

# Application Configuration
NODE_ENV=development
APP_PORT=3000

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5MB

# Security
SESSION_SECRET=your-secret-key-here
CORS_ORIGIN=http://localhost:8080

# XAMPP Default Settings
# If you're using XAMPP, these are typically the default settings:
# - MySQL runs on port 3306
# - Default user is 'root' with no password
# - Make sure MySQL service is running in XAMPP Control Panel
